services:
  - type: web
    name: kirei-chat-app
    env: docker
    repo: https://github.com/kirei-io/middle.messenger.praktikum.yandex.git
    branch: sprint_4
    healthCheckPath: /
    buildCommand: docker build -t chat-app -f Dockerfile .
    startCommand: docker run -p 3000:3000 -it chat-app
